{
  "name": "zerocreds-mcp",
  "version": "0.1.0",
  "description": "MCP server for ZeroCreds — credential collection without exposing values to the LLM",
  "type": "module",
  "bin": {
    "zerocreds-mcp": "./index.js"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
